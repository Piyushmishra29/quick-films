# Heavy video source — exceeds GitHub's 100MB file limit. Store out-of-band.
assets/videos/

# Scraped third-party reference site (copyright) — local only.
reference/

# OS / tooling
.DS_Store
node_modules/

# build intermediates (duplicates of web/public/films)
assets/web-encodes/

# local review screenshots (heavy, regenerate anytime)
docs/review/

# client source stills (124MB) — web-optimized copies live in web/public/stills
assets/stills/
.gstack/
